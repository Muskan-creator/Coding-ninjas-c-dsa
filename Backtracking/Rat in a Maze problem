#include<bits/stdc++.h>
using namespace std;
#define 


void solvemaze(int maze[n][20],int n){
    solvemazehelper(in)
}
int main(){
    int n;
    cin>>n;
    for(int i=0;i<n;i++){
        for(int j=0;j<n;j++){
            cin>>maze[i][j];
            
        }
    }
      solveMaze(maze,n);
}
